import { PaneDirective } from '../directives/pane.directive';

describe('PaneDirective', () => {
  it('should create an instance', () => {
    const directive = new PaneDirective();
    expect(directive).toBeTruthy();
  });
});
